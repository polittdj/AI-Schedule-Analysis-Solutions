/* ==========================================================================
 * Schedule Forensics — Tabulator.js placeholder
 * --------------------------------------------------------------------------
 * This file is intentionally empty. Tabulator.js is loaded from the CDN
 * fallback <script> tag in base.html. For air-gapped deployments, download
 * the real UMD bundle + matching CSS and save them here to replace the
 * placeholders:
 *
 *   curl -o app/web/static/lib/tabulator.min.js \
 *     https://unpkg.com/tabulator-tables@5.5.2/dist/js/tabulator.min.js
 *   curl -o app/web/static/lib/tabulator.min.css \
 *     https://unpkg.com/tabulator-tables@5.5.2/dist/css/tabulator.min.css
 *
 * Then remove the CDN <script>/<link> tags from base.html so the local copy
 * is the only source.
 * ========================================================================== */
if (typeof window !== "undefined" && typeof window.Tabulator === "undefined") {
  console.info("[schedule-forensics] Tabulator.js placeholder — CDN fallback will load.");
}
