/* ==========================================================================
 * Schedule Forensics — Chart.js placeholder
 * --------------------------------------------------------------------------
 * This file is intentionally empty. Chart.js is loaded from the CDN fallback
 * <script> tag in base.html. For air-gapped deployments, download the real
 * UMD bundle and save it here to replace the placeholder:
 *
 *   curl -o app/web/static/lib/chart.min.js \
 *     https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js
 *
 * Then remove the CDN <script> tag from base.html so the local copy is the
 * only source. The air-gapped checklist in the project README tracks this.
 * ========================================================================== */
if (typeof window !== "undefined" && typeof window.Chart === "undefined") {
  console.info("[schedule-forensics] Chart.js placeholder — CDN fallback will load.");
}
